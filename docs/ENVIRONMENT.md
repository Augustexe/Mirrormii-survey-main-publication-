# Project environment

## Local source and artifacts

Project ID: `mirrormii-genie-survey`.
Canonical checkout: `/Users/jerryzhang/Workspace-Draft/projects/mirrormii-genie-survey/project`.
Runs: `/Users/jerryzhang/Workspace-Draft/projects/mirrormii-genie-survey/runs`.

The registered runtime is `static-content` for planning. No application framework, deployment service, or production database adapter has been selected. Local Git uses `main`; the remote GitHub owner is pending.

Work can proceed with Markdown, JSON, and standalone HTML. Teammates can read the proposed spec with no production credentials or dependency install. Git stores the proposed product source; Lark remains company authority and the intended operational data destination.

## GitHub handoff

Prepare a private repository named `mirrormii-genie-survey` in the owner/org supplied by Jerry. The scaffold includes a pull request template and planning issue template. No collaborators, team permissions, secrets, Actions workflows, deployments, or remote repository have been provisioned.

The native `gh api user --jq .login` connectivity check failed on 2026-09-14. Do not treat this as evidence of missing credentials or reauthenticate automatically. Preserve native authentication. Once owner and connectivity are available, create the private repository, push reviewed planning source, and verify the remote URL and visibility. Ask for collaborator handles only when actual invitations are in scope.

## Company context route

Use the existing protected company-context service through the central workspace, or native authorized Lark read tooling. Load only Product Truths, Sayable Claims, Brand Facts, relevant ICP/campaign context, and applicable Wiki documents. Inspect source status, revision/time, and scope before synthesis. Do not export raw packets into this repo or shared manifests.

Checked 2026-09-14: protected context service unavailable; native CLI could not initialize keychain access in this execution context; existing browser tab access was denied by Browser Use security policy. No fresh company contents were verified. Stop that browser route until access is explicitly restored; do not work around the denial.

`context/source-map.json` contains routing references, not live company truth. Future agents must refresh them before treating content or schema as verified.

## Asset route

The canon Eagle library exists at `/Users/jerryzhang/Workspace-Draft/assets/Genomii AI.library`. No Genie master item has been selected. Wait for founder visual direction, then resolve approved assets through Eagle metadata, IDs, folders and rights. Record selected asset provenance in the corresponding run output.

The first visual planning board uses a neutral review theme. It is not a proposed website design or substitute character artwork.
